# ==================================================
# ENVIRONMENT: PREPROD (running on PROD infrastructure)
# ==================================================

spring:
  profiles: prod

  jackson:
    time-zone: Europe/Madrid
    serialization:
      INDENT_OUTPUT: true

  datasource:
    url: jdbc:mysql://10.102.1.57:3306/qcloud_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: admin
    password: CRG4dm1n!

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: MySQL57InnoDB
    show-sql: false

  flyway:
    enabled: true
    validate-on-migrate: true
    baseline-on-migrate: false
    out-of-order: false

  h2:
    console:
      enabled: false

  mail:
    default-encoding: UTF-8
    host: smtp.crg.eu
    username:
    password:
    port: 25
    protocol: smtp
    test-connection: false
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

  main:
    allow-bean-definition-overriding: true

server:
  port: 8088

application:
  version: PROD

management:
  endpoint:
    health:
      show-details: always

jwt:
  header: Authorization
  secret: L+0P1i+38a26pHiW10iHqPBYAyklvqPdySy98BV9PPCZyDogMggoSZkYxULqCF+WG6Nimlfxd53H6zjdIUJNJA==
  expiration: 604800
  route:
    authentication:
      path: api/auth
      refresh: refresh
    authentication-pipeline:
      path: api/4uth
      refresh: refresh

qcloud:
  app-url: https://qcloud2.crg.eu
  admin-email: roger.olivella@crg.eu
  tip-refresh: 1800000

  threshold:
    min-points-auto: 10
    min-points-manual: 20
    min-points-guideset: 3
    min-valid-context-source-points: 2
    files-auto-plot: 10

  threshold-nonconformity:
    page-size: 10

  instrument-status:
    max-offline-hours: 12

  email:
    address: roger.olivella@crg.eu

  intranet:
    pipeline-valid-hours: 3
    api-url: https://api.qcloud2.crg.eu

twitter:
  consumer-key: dummy
  consumer-secret: dummy
  access-token: dummy
  access-token-secret: dummy

logging:
  file:
    name: /home/admin/qcloud/logs/qcloud2_docker_prod.log
  level:
    org.springframework.security: ERROR
    org.hibernate: ERROR

