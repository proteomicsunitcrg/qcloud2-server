# config context path to "/" by setting an empty string
server:
  contextPathContextPath:

# mail properties
  
application:
  version: dev0.0.1(March demo)  




# JACKSON
spring:
  jackson:
    time-zone:
      Europe/Madrid
    serialization:
      INDENT_OUTPUT: true
  datasource:    
    ## MYSQL
    url: jdbc:mysql://localhost:3306/qcloudd
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: MySQL57InnoDB
  
  
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: daniel.mancera.crg
    password: c
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp
    test-connection: false


jwt:
  header: Authorization
  secret: th1515th3n3wqcloudtwopointzeroo
  expiration: 604800
  route:
    authentication:
      path: api/auth
      refresh: refresh
      

logging:
  level:
    org.springframework:
      security: DEBUG
#    org.hibernate:
#      SQL: DEBUG
